# SDD progress — M9 + M10 + notes test app (session 2026-07-11)

Phase 0 (parallel): M9 plan author (opus), M10 plan author (opus), notes test-app implementer (sonnet) — dispatched.
M9 plan: committed 4a96a39 (authored by opus, reviewed by architect).
M9 T1 (wave 0, build.zig+flake.nix) dispatched; base commit caee94d.
M10 plan: committed 5eab0a2 (authored by opus, reviewed by architect).
M10 T5 (wave A, tools/codegen.ts) dispatched; base commit 5eab0a2.
M9 T1 complete (commit 859cbca, architect-reviewed clean). Wave 1 (T2/T3/T4) dispatched.
M10 T5 complete (commit 2f74c3f, architect-reviewed clean).
M10 Wave B dispatched: T2 (src/ndp_binary.zig), T3 (runtime binary encoder), T6 (src/acl.zig), T8 (nd_plugin.h + plugins/hello). build.zig addTest wiring reserved for architect post-wave.
Owner msg relayed to notes agent: GNOME/Apple-native sidebar expectation; task #7 created for native chrome widgets.
M9 T2 complete (commit 2afdd36, architect-reviewed clean; page_allocator deviation accepted; NOTE: sample.manifest.json fixture replaced by inline-JSON tests — acceptable). Git-index race hit+fixed by T2; future dispatches must commit with explicit pathspecs.
M10 T6 complete (f0eea30, acl.zig std-only, 6 tests green via direct zig test). Its zig-build-test failure concern = stale-runner noise; architect re-ran: EXIT=0, 71/71.
M10 T8 complete (5bddb99, nd_plugin.h + plugins/hello; c_allocator deviation accepted; T9 must ensure libc link on plugin artifact).
M9 T4 complete (c889055+29f92b3, mac-verified; brew minisign installed on mac — machine state note for T6/T7).
M10 T2 complete (8b818f7; SPEC OFF-BY-ONE found: stringTableOffset=82 not 83; T3 notified, spec fix pending architect).
Architect wired ndp_binary+acl test roots into build.zig; zig build test EXIT=0.
M9 T3 complete (da35411; mksquashfs fallback used — appimagetool absent; real fix: bundle workspace root + bun install --production in AppDir). M9 Wave 1 DONE.
M10 T3 complete (93f7763; independent 82 derivation, byte-identical to T2 fixture). M10 Wave B DONE.
Spec off-by-one fixed by architect (op[0]=18 not 19; 82/135). T7 dispatched next.
M9 T5 complete (ea000eb; M9_UPDATE_OK first attempt). Minor for final review: unused readFileSync import in scripts/m9-drive.ts (verbatim from plan).
M9 T6 complete (89b48b9+6249d39; linux leg green by implementer; mac leg BLOCKED on stray file -> architect cleaned mac, ran mac-m9.sh: MAC_M9_OK EXIT=0).
M10 T7 impl complete but UNCOMMITTED pending encoder array-prop fix (architect decided per-batch JSON fallback; fix agent dispatched).
Important for fix wave: packers not idempotent vs leftover dist/ (T6 worked around with rm -rf).
Encoder fix db50b1f landed (per-batch JSON fallback; m5b/m5c/m3 green w/ binary). T7 agent resumed to commit held diff.
Known limitation recorded: gallery batches all touch array prop -> 100% JSON fallback there; array/object wire tag deferred (candidate follow-up).
M9 T7 complete (ce7f26c + 97a3441). M9 MILESTONE COMPLETE: T1 859cbca, T2 2afdd36, T3 da35411, T4 c889055+29f92b3, T5 ea000eb, T6 89b48b9+6249d39, T7 ce7f26c+97a3441. headless-m9 green, MAC_M9_OK. Full gate re-run deferred to session end.
M10 T7 committed (c3bcfca, 122/122 + 7 headless scripts green). Wave C remainder dispatched in parallel: T9 (plugin loader, owns build.zig+runtime.zig now), T10 (nd.h+abi.zig), T4 (bench-10k, median-of-3).
M10 T4 complete (8675942; bench green as gate but binary 3.6x SLOWER than json: 9.4 vs 2.6ms median). Perf-fix agent dispatched for runtime/ndp-binary.ts ByteWriter (target <=2x, byte-identical output).
T4 findings for T14/activeContext: 10k-tree paint segfaults cairo SHM after markers (pre-existing GTK limit); bun test multi-file summary display quirk; T4 also added 1-line ND_FORCE_JSON to runtime/ndp.ts (accepted).
Session-limit kill recovery: T9/T10 work verified green in tree (143/143 w/ plugin .so, symbols in libnd.a, m3 green) and committed by architect (be3b611, bc7425c). Perf edit verified (golden byte-exact) + benched: binary 9.4->6.5ms median vs json 2.8 => ~2.3x; committed. ACCEPTED RATIONALE: TS-encode-only measurement vs native JSON.stringify; ~4ms absolute per 10k mount; smaller wire bytes; host decode win unmeasured. T14 bench gate: completion + informational ratio, generous bound (4x).
Notes app committed f4e6ee9 (ND_NOTES_OK green; architect ran drive + inspected screenshot).
NOTES VERDICT: bespoke-styled app shippable with friction; GNOME-native look NOT achievable today (no chrome widgets, no cssClasses hatch, no AdwApplication/dark-mode). 2 framework bugs found: update-op empty kind (CRITICAL, tree.zig:211), gtk insertBefore reverse-order + parented-move assert (HIGH). Bugfix agent dispatched (task #8). Chrome widgets = task #7.
M10 T12 (d0ea5be) + T13 (c04ada2) complete. Remaining: T11 (mac compat, running), notes bugfix agent (running), then T14 (headless-m10 + CI + activeContext) last.
M10 T11 complete (29c1b1a, MAC_M6_OK full gate w/ new ABI). Header-install gap found by T11 fixed by architect (2314d6f, HEADER_TREE_OK). Waiting: notes bugfix agent; then T14.
Notes bugfixes landed: 2ba8701 (tree update-kind, red->green conformance test) + 1ff729d (gtk reorderChildAfter). REVIEW CATCH: 1ff729d hand-edited the GENERATED widgets.zig — architect ported fix into tools/codegen.ts (465106f), FRESHNESS_OK verified.
Bugfix agent extra finding: automation getTree child order = hashmap iteration order, not GTK sibling order (pre-existing; candidate follow-up).
M10 MILESTONE COMPLETE. Full 14-leg Linux gate green + MAC_M6_OK + MAC_M9_OK against HEAD. Final review + chrome-widgets plan dispatched.
FINAL REVIEW (opus): mergeable as-is; 0 Critical, 3 Important (I1 null-backend move dup, I2 acl @intCast panic, I3 writeRawJson unvalidated splice), 5 Minor. Fix agent dispatched for I1-I3 + M5 + M8.
Recorded-only minors: M4 manifest replay/downgrade (design follow-up w/ delta milestone), M6 process-global plugin registry (multi-plugin hazard), M7 before==0 sentinel ambiguity in binary insert (unreachable today).
Final fixes landed (a20b925..5bb357a, red->green). activeContext: 82/135 correction + session record appended. Memory saved. Awaiting M11 plan author.
M11 plan committed 79fb0c1 (stale descopes corrected by architect). Wave 0 (task 0 Adw bindings regen) + parallel task 3 (getTree ordering) dispatching.
M11 T3 complete (029b79c; + GC-sweep unlink fix beyond brief; 147/159 green; NOTE for T9: notes-drive.ts stale comment re order assertions — tighten pin leg). T0 still running. T2 dispatched.
M11 T0 complete (07f359d + af64cee): Adw-1 bindings regenerated, module name adw1 (imported as adw). CORRECTIONS for T5/T7/T8 dispatches: -Dmodules is a REPEATED flag not comma-joined; module is adw1 not adwaita1. Verified OverlaySplitView/HeaderBar/WindowTitle/init exist.
T2 running (owns codegen); T4 then wave 2 (T5+T6) queue behind it — T5 touches the STRUCTURAL emitter table so codegen stays single-owner.
=== PAUSED 2026-07-11 (owner request) ===
main pushed to mac at 2ccf37b; uncommitted wave-2 WIP snapshotted as branch wip/m11-wave2 (2c76cfa), working tree left untouched for the possibly-still-active other session.
Mac: ~/Development/NativeDesktop clone @ 2ccf37b + CLAUDE-activeContext.md + SESSION-LEDGER.md copied (both gitignored, hence manual copy).
M11 remaining: T4 (TextArea floor), T5 (AdwApplication+dark mode), T6 (cssClasses GTK applier), T7 (SplitView), T8 (HeaderBar), T9 (notes restyle+ND_NAVCHROME_OK), T10 (mac leg+gate). Plan: docs/superpowers/plans/2026-07-11-m11-native-chrome.md. Facts: adw module=adw1, -Dmodules repeated flag.
=== RESUMED 2026-07-11 overnight, ON THE MAC (~/Developer/NativeDesktop) ===
Owner feedback: T5 (wip) poorly implemented — AdwApplicationWindow swap strips titlebar before HeaderBar exists; REDO with AdwToolbarView + default top bar. Owner directive: native chrome must be REAL on macOS too (NSSplitView/titlebar integration/semantic classes), not best-effort-degraded. Use subagents.
Mac setup fixed: nix libadwaita BROKEN on darwin (appstream dep) -> devshell shellHook borrows brew pkgconfig; brew libadwaita 1.9.2 + gtk4 4.22.4 installed (machine state). zig build test EXIT=0 on mac (147/159, 12 skip, parity w/ linux). QUIRK: GTK-linked test binaries print spurious "failed command ... --listen=-" then pass on rerun — trust exit code. FIX: overlay.zig fn-ptr casts needed @alignCast on aarch64 (f11bde1). Full `zig build` (GTK exe) also compiles+links on mac via brew — GTK arms of T5-T8 are compile-verifiable HERE.
wip/m11-wave2 reconciled: layout props landed (9068546, w/ docs label fix); weak T5 discarded; notes restyle HELD on branch as T9 reference. Wave A dispatching: T4 (codegen/schema owner) || T6 (embedder+swift, NO codegen — cssClasses rides existing props JSON through frozen 18-field vtable; core untouched).
Wave A done: T4 TextArea floor (1de8dfe, both backends, all gates green). T6 cssClasses (ea1a88c): gtk addCssClass verbatim (embedder-side wiring in gtk/backend.zig vtCreate+vtApplyProps; NO core changes, vtable untouched); appkit REAL semantic mapping (accent/destructive buttons, preferredFont type styles, secondaryLabelColor, monospaced; structural classes deliberately ignored — chrome widgets own those). Deviations accepted: allocPrintZ doesn't exist in zig 0.16 -> gpa.dupeZ; MAC FACT: swift build needs `env -u SDKROOT -u DEVELOPER_DIR` (nix leaks stale SDK 14.4 into shell, breaks system swift 6.3.1).
T5 REDO done (e2a54ef): AdwApplication swap ONLY, window class untouched -> titlebar survives (the rejected wip swapped the window class and stripped chrome). RUNTIME PROOF ON MAC: nd-hello --smoke boots via gtk4 quartz backend, ND_SMOKE_MAPPED, exit 0 (benign GSettings schema warning from brew stack). docs/agents/styling.md gained cssClasses + dark-mode sections.
T7 done (ad3d8b1): SplitView = AdwOverlaySplitView (gtk) / REAL NSSplitView + .sidebar NSVisualEffectView vibrancy wrapper (mac, holding priority 260, sidebar always pane 0). slot attached prop: protocol.Attached + tree.zig dupe/free + CRITICAL abi_backend.zig attachedJsonZ Shape (without it slot never crosses the vtable — agent caught it). SplitView = widget-type 20 (schema APPEND-ONLY, positional IDs). Conformance slot test added. Note for visual pass: mac sidebar initial width = fraction*900 defaultLow constraint (pragmatic).
T8 dispatched: headerbar -> gtk_window_set_titlebar(AdwHeaderBar) when child of window (gobject.ext.isA branch in STRUCTURAL.Window); mac = REAL unified NSToolbar via new NDShell/HeaderBar.swift (NDGen+NDShell are ONE swift target — ndGridPlace precedent). HeaderBar = widget-type 21. T9 queues behind T8 (needs generated intrinsics for the restyle).
T8 done (963cbfd): headerbar = real window titlebar on gtk (setTitlebar + gobject.ext.isA branch), REAL unified NSToolbar on mac (NDShell/HeaderBar.swift: NDToolbarManager full-recreate rebuild, NDHeaderBarView marker class). BONUS FIX: collectAttachedProps now UNIONS same-named enum attached props (slot: sidebar|content|start|end) — previously first-declaration silently shadowed later ones.
T9 restyle WRITTEN (uncommitted, 4 files) + first-ever native mac run of notes FOUND 2 PRE-EXISTING APPKIT FRAMEWORK BUGS (latent till now, exactly like the linux notes probe found 2): (1) BLOCKING scrollview documentView never constrained -> 0x0 forever -> children "offscreen"/not actionable (Widgets.swift ScrollView append, sv.documentView = child bare); (2) window create hardcodes 480x320, ignores defaultWidth/Height. Chrome itself PROVEN on mac: headerbar+splitview mount, roles correct, toolbar button clickable, dark mode automatic (screenshot inspected by architect). Fix agent dispatched (codegen owner, red->green native drive required). KNOWN GAP recorded: hexpand/vexpand/halign/valign have no AppKit mapping (style keys silently ignored on mac) — acceptable, splitview/autoresizing carries the notes layout; candidate follow-up.
Framework-bug fix landed (e321087, red->green): scrollview FlippedView stays documentView pinned to clipView (width==clip, top-anchored), child pinned inside; window honors defaultWidth/Height (swiftDefaultInt emitted schema default as literal, never a propInt read). Gallery regression-swept: M6_GALLERY_OK 100k items locally.
T9 committed (83e4b43) after fix proved it green. T10 done: mac-m11.sh dual-mode wrapper + mac-m11-body.sh (runs locally on Darwin, ssh from linux) committed 8a5ba15 + mac.yml non-blocking step; MAC_M11_OK green locally. Combined mac gate at HEAD: codegen freshness + zig build test + zig build + bun tests ALL EXIT=0.
BONUS: full notes drive ALSO green under the GTK backend natively on macOS (quartz gdk) — ND_NOTES_OK + ND_NAVCHROME_OK at 900x600, real AdwHeaderBar titlebar + OverlaySplitView + auto dark mode; architect inspected screenshot: reads as a real GNOME app. GTK side of M11 is runtime-verified before touching linux.
M11 COMPLETE ON THE MAC. NOT PUSHED (15 local commits on main). Remaining for the linux box: push/pull, full 14-leg weston gate + headless-notes (now also requires ND_NAVCHROME_OK). activeContext updated with the full M11 entry incl. mac machine facts.

# Session 2026-07-12 (on the Mac): appkit layout polish (post-dual-header)
Dual-header follow-up: the 3 owner-reported mac gaps (traffic-light overlap, density, squished rows) all traced to the appkit backend never implementing the box-layout style keys. Landed c5b644e (sonnet implementer, architect-reviewed + independently re-verified):
- NEW NDShell/Layout.swift: ndLayoutFlags recorded at style time, consulted at attach (tree.zig styles BEFORE append); main-axis expand = hugging priority 1 + stack distribution .fill (restored to .gravityAreas when last expander leaves); cross-axis halign/valign fill = tracked 999 width/height constraint inset by stack edgeInsets (center/end = ND_WARN v1 gap); NDButton/NDTextField inflate intrinsicContentSize by ndPadding (padded .rounded bezel auto-switches to .flexiblePush, macOS 14+); NDPaneHostView wraps the splitview content pane.
- Backend.swift ndApplyStyle rewrite fixed THREE latent bugs: font subkeys read size/family but schema sends fontSize/fontFamily/fontWeight (fonts NEVER applied on mac till now); border subkeys same (borderWidth/borderColor/borderRadius); scalar padding silently dropped + object padding went through a one-shot frame hack that fought Auto Layout (deleted). padding now dispatches: NSStackView edgeInsets / NDButton+NDTextField ndPadding / TextArea textContainerInset / ScrollView contentInsets. margin = documented no-op on appkit v1.
- codegen.ts swift arms: Box append/insert/remove call ndBoxChildAttached/Detached (BOX_CROSS_FILL_BODY removed); ScrollView/TextArea minContentHeight becomes a REAL >=999 heightAnchor floor (frame set is ignored once arranged, translatesAutoresizingMask off); SplitView sidebar content pinned to wrapper.safeAreaLayoutGuide.topAnchor (vibrancy still reaches the top, content clears traffic lights); content pane wrapped in NDPaneHostView with same safe-area pin; remove arm wrapper-detection generalized. Only Widgets.swift changed among generated outputs (freshness clean).
- VERIFIED (architect re-ran everything): MAC_M11_OK; geometry probe note-row h=40 (was ~24), note-list-scroll h=471 (vexpand fills), search-input y=64 + title-input y=72 (both panes clear the ~52pt safe area); M6_DRIVE_OK + M6_GALLERY_OK itemCount=100000 regression sweep. Screenshot caveat stands: offscreen captures miss _NSCoreHostingView content on macOS 26 — geometry via getTree is the assertable signal; owner confirms visuals headful.
- Still pending (unchanged): linux box push + full 14-leg weston gate + headless-notes w/ ND_NAVCHROME_OK. Zig sources untouched this session so linux risk unchanged.
